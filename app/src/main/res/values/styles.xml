<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SpinnerStyle" parent="android:Widget.ListView">
        <item name="android:divider">@color/colorPrimaryDark</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:background">@drawable/spinner_item_border</item>
        <item name="com.fortuna.android.mobilecustomer:fontBro">fonts/Arial.ttf</item>
        <item name="android:height">48dp</item>
    </style>

    <style name="my_button" parent="@android:style/Widget.Button">
        <item name="com.fortuna.android.mobilecustomer:fontBro">fonts/Arial.ttf</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
